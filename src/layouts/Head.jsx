import React from 'react';
import { Layout } from 'antd'
import './Head.less'

const { Header } = Layout;

const Head = () => (
    <Header className="air-header">机场协同放行</Header>
);

export default Head;